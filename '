<div class="row">
  <div class="jumbotron jumbo-survey jumbo-pic">
    <div class="survey">
      <div class="container">
        <%= form_tag complete_survey_path, method: :post, remote: :true do %>
          <%= label @question_type, @question %>
          <br>
          <div class="btn-holder col-xs-8">
            <%= submit_tag "Yes", class: "yes btn btn-lg btn-success" %> 
            <%= submit_tag "No", class: "no btn btn-lg btn-danger" %> 
          </div>
        <% end %>

        <div class="btn-holder col-xs-8">
          <%= form_tag email_form_survey_path, method: :post, remote: :true do %>
            <%= email_field_tag "email", class: "form-control", placeholder: "email@example.com" %>

            <% hidden_field_tag @theme %>
            <%= submit_tag "Send", class: "btn btn-default" %>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>
